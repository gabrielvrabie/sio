{% extends "base.html.twig" %}
{% block body %}
    <p>Create record</p>
    <div class="container-fluid">
        {{ form_start(form, {action:path('new_log_time')}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
